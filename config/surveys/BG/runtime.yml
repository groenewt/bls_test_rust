config_version: 1
feature_flags:
  enable_hot_reloads: false
  use_memory_map_series: true
  enable_checkpoint: false

environment_overrides:
  - key: "BLS_BG_MEMORY_LIMIT_GB"
    maps_to: "resources.memory_limit_gb"
  - key: "BLS_OUTPUT_FORMAT"
    maps_to: "format"
  - key: "BLS_PARTITION_STRATEGY"
    maps_to: "partition_hints.strategy"

logging:
  level: "info"
  structured: true

error_handling:
  on_error: "continue"
  max_errors: 1000
  log_errors: true
  error_file: "bg_errors.log"
  
  checkpoint:
    enabled: false
    interval_records: 10000
    checkpoint_dir: "checkpoints/bg"

# Performance monitoring
monitoring:
  enabled: true  # Required field for MonitoringConfig
  metrics_endpoint: null
  health_check_interval: 60
