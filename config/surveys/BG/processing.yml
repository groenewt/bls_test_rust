config_version: 1
strategy:
  mode: "in_memory"
  max_concurrency: 1

resources:
  max_threads: 2
  memory_limit_gb: 2

io:
  chunk_size_mb: 32
  buffer_pool_size: 2

features:
  use_mmap: false
  enable_statistics: true
  enable_profiling: false

transforms: []