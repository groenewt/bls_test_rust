# PD Survey DAG Configuration
# Pipeline DAG with tasks, dependencies, retries, and SLAs
config_version: 1

graph:
  tasks:
    - id: "discover"
      type: "scan"
      inputs: []
      retries: { max: 3, backoff_sec: 15 }
      sla_minutes: 10
    - id: "validate"
      type: "validate_model"
      inputs: ["discover"]
      retries: { max: 2, backoff_sec: 10 }
      sla_minutes: 20
    - id: "plan"
      type: "partition_plan"
      inputs: ["validate"]
      retries: { max: 2, backoff_sec: 10 }
      sla_minutes: 15
    - id: "process"
      type: "execute"
      inputs: ["plan"]
      parallelism: 4
      retries: { max: 3, backoff_sec: 60 }
      sla_minutes: 180
    - id: "write"
      type: "output"
      inputs: ["process"]
      retries: { max: 3, backoff_sec: 30 }
      sla_minutes: 90
    - id: "qc"
      type: "quality_checks"
      inputs: ["write"]
      retries: { max: 2, backoff_sec: 20 }
      sla_minutes: 30

  edges:
    - from: "discover"
      to: "validate"
    - from: "validate"
      to: "plan"
    - from: "plan"
      to: "process"
    - from: "process"
      to: "write"
    - from: "write"
      to: "qc"

# Global DAG settings
settings:
  max_active_runs: 1
  catchup: false
  depends_on_past: false
  email_on_failure: true
  email_on_retry: false
  retries: 1
  retry_delay_minutes: 5
