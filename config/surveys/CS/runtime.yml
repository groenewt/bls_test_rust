# CS Survey Runtime Configuration
# Runtime settings, feature flags, and environment overrides
config_version: 1
feature_flags:
  enable_hot_reloads: false
  use_memory_map_series: true
  enable_checkpoints: true
  enable_error_recovery: true

environment_overrides:
  - key: "BLS_CS_MEMORY_LIMIT_GB"
    maps_to: "resources.memory_limit_gb"
  - key: "BLS_CS_MAX_THREADS"
    maps_to: "resources.max_threads"
  - key: "BLS_OUTPUT_FORMAT"
    maps_to: "format"
  - key: "BLS_PARTITION_STRATEGY"
    maps_to: "partition_hints.strategy"
  - key: "BLS_CS_MAX_ERRORS"
    maps_to: "policy.max_errors"
  - key: "BLS_CS_CHUNK_SIZE_MB"
    maps_to: "io.chunk_size_mb"

logging:
  level: "info"  # trace|debug|info|warn|error
  structured: true

error_handling:
  on_error: "continue"  # Continue processing other records
  max_errors: 5000      # Stop if too many errors (increased for larger dataset)
  log_errors: true
  error_file: "cs_errors.log"

checkpoint:
  enabled: true  # Enable checkpoints for large-sized data
  interval_records: 100000
  checkpoint_dir: "checkpoints/cs"
# Performance monitoring
monitoring:
  enabled: true  # Required field for MonitoringConfig
  metrics_endpoint: null
  health_check_interval: 60
