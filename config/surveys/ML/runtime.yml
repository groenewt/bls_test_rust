# ML Survey Runtime Configuration
# Feature flags, env overrides mapping, logging
config_version: 1

feature_flags:
  enable_hot_reloads: false
  use_memory_map_series: false
  enable_checkpoints: true
  enable_parallel_processing: true

environment_overrides:
  - key: "BLS_ML_MEMORY_LIMIT_GB"
    maps_to: "resources.memory_limit_gb"
  - key: "BLS_ML_MAX_THREADS"
    maps_to: "resources.max_threads"
  - key: "BLS_ML_CHUNK_SIZE_MB"
    maps_to: "io.chunk_size_mb"
  - key: "BLS_ML_MAX_ERRORS"
    maps_to: "policy.max_errors"
  - key: "BLS_OUTPUT_FORMAT"
    maps_to: "format"
  - key: "BLS_PARTITION_STRATEGY"
    maps_to: "partition_hints.strategy"

logging:
  level: "info"
  structured: true

checkpoint:
  enabled: true
  interval_records: 25000
  checkpoint_dir: "checkpoints/ml"

error_handling:
  on_error: "continue"
  max_errors: 1000
  log_errors: true
  error_file: "ml_errors.log"
# Performance monitoring
monitoring:
  enabled: true  # Required field for MonitoringConfig
  metrics_endpoint: null
  health_check_interval: 60
