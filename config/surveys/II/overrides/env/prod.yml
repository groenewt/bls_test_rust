# II Survey Production Environment Overrides
config_version: 1

# Production environment specific settings
resources:
  max_threads: 8
  memory_limit_gb: 16

logging:
  level: "warn"

features:
  enable_profiling: false

# Production error thresholds
error_recovery:
  max_errors: 5000